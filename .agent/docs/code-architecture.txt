project-root/
│
├── app/
│   ├── __init__.py
│   ├── main.py              # Điểm khởi tạo FastAPI app
│   ├── core/                # Cấu hình hệ thống (Config, Security, Logging)
│   │   ├── config.py
│   │   └── security.py
│   │
│   ├── api/                 # Giao diện HTTP (Controllers/Routes)
│   │   ├── v1/
│   │   │   ├── api.py       # Gom tất cả router v1
│   │   │   └── endpoints/   # Tách router theo domain (order, product, user)
│   │
│   ├── domains/             # Lớp nghiệp vụ (Core Logic) - Quan trọng nhất
│   │   ├── orders/
│   │   │   ├── models.py    # Database Models (SQLAlchemy/Tortoise)
│   │   │   ├── schemas.py   # Pydantic models (Data Validation)
│   │   │   ├── services.py  # Logic xử lý nghiệp vụ chính
│   │   │   └── repository.py# Tương tác trực tiếp với DB
│   │
│   ├── dependencies/        # Nơi chứa các Dependency Injection (DI)
│   │   ├── database.py      # get_db session
│   │   ├── redis.py         # get_redis_client
│   │   └── auth.py          # get_current_user
│   │
│   └── infrastructure/      # Các tích hợp bên ngoài (Kafka, S3, Gateway)
│       └── kafka_client.py
│
├── tests/                   # Unit test & Integration test
├── .env                     # Biến môi trường
├── docker-compose.yml       # Điều phối container
└── Dockerfile